version: "3.9"

services:
  test-runner:
    image: python:3.11-slim
    working_dir: /work
    volumes:
      - ./:/work
      - pip-cache:/root/.cache/pip
    command:
      - sh
      - -c
      - python -m pip install -r quark_strm/requirements.txt && tail -f /dev/null

volumes:
  pip-cache:
