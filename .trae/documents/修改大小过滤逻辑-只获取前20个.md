## 修改计划

修改 `quark_strm/app/services/search_service.py` 中的 `_apply_size_filter` 方法：

1. 只获取评分最高的前20个夸克资源的大小
2. 其他资源默认保留（不过滤）
3. 使用用户提供的 Cookie 尝试获取大小

## 修改内容

1. 修改 `_apply_size_filter` 方法：
   - 按评分排序，只取前20个夸克资源
   - 批量获取这20个资源的大小
   - 无法获取大小的资源默认保留

2. 修改 `quark_size_fetcher.py`：
   - 添加使用 Cookie 的请求头

请确认是否执行？