## 调试计划

用户反馈选择其他网盘后还是只显示夸克资源。需要边测试边解决，同时检查前后端输出。

### 步骤

1. 启动后端服务（使用 PowerShell 兼容语法）
2. 测试 API 确认后端是否正常
3. 检查前端请求参数
4. 根据测试结果修复问题

### 命令

```powershell
cd quark_strm
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

请确认是否执行？