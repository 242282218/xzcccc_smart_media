## 执行计划

1. 启动后端服务
2. 测试 API 参数解析
3. 验证修复效果

## 命令

```bash
# 1. 启动后端服务
cd quark_strm && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 2. 测试 API
python test_fastapi_parse.py
```

## 预期结果
- 后端服务正常启动
- FastAPI 正确解析 cloud_types 参数
- 多网盘搜索正常工作

请确认是否执行？