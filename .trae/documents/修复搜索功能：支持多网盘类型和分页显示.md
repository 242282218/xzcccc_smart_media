## 修复计划

### 目标
修复首页搜索资源只显示20个夸克链接的问题，支持所有网盘类型并完善分页显示。

### 智能体分工
- **编程专家**: 修改后端搜索源和前端页面
- **测试专家**: 验证修复结果

### 执行步骤

#### 步骤1: 修复 QuarkAPISource 网盘类型过滤（后端）
**文件**: `quark_api_package/packages/search/sources/quark_api.py`
- 修改 `_parse_resource` 方法，移除 `"pan.quark.cn" not in link` 的硬编码过滤
- 使用 `link_extractor` 提取所有类型的网盘链接
- 确保返回的结果包含所有网盘类型

#### 步骤2: 修复前端分页显示
**文件**: `quark_strm/web/src/views/SearchView.vue`
- 添加分页组件（页码导航）
- 修改 `page_size` 从固定20改为可配置（默认20，支持切换）
- 完善网盘类型筛选选项（添加115、123、天翼等）

#### 步骤3: 修复前端网盘标签显示
**文件**: `quark_strm/web/src/views/SearchView.vue`
- 完善 `getCloudTypeLabel` 函数，支持所有网盘类型
- 添加缺失的网盘类型映射（115、123、天翼等）

#### 步骤4: 验证修复
- 测试搜索是否返回多种网盘类型
- 测试分页功能是否正常工作
- 测试页码跳转和每页数量切换

### 风险提示
1. QuarkAPI 本身可能只返回夸克链接，需要确认数据源
2. 修改分页逻辑可能影响现有用户体验
3. 需要确保前端和后端分页参数一致

### 回滚方案
```bash
git reset --hard <锚点>
```

请确认此计划后，我将开始执行修复。