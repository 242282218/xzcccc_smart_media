# RULES.md — 强制规则与治理细则

## 术语级别定义
- `MUST`：强制要求，必须满足。
- `SHOULD`：建议要求，默认应满足；若不满足需有明确理由。
- `MAY`：可选要求，按场景决定是否采用。

## 一、规则链路与裁决优先级
1. 入口文件：`AGENTS.md`（作用域与索引）。
2. 执行框架：`AGENT.md`（流程与决策框架）。
3. 强制规则：`RULES.md`（必须做/禁止做）。
4. 编码约定：`CONVENTIONS.md`（工程实现标准）。

冲突裁决：
- `RULES.md` 与 `CONVENTIONS.md` 冲突时，`RULES.md` 优先。
- `AGENT.md` 只定义流程，不得覆盖 `RULES.md`。
- `AGENTS.md` 只做入口索引；若不一致，以 `AGENT.md` + `RULES.md` 为准。
- 未覆盖场景按 `CONVENTIONS.md` 与最小改动原则执行。

## 二、沟通与交互
1. `[MUST]` 与用户交流时使用中文。
2. `[MUST]` 遇到不确定事实先验证（联网搜索或代码检索）再下结论。
3. `[SHOULD]` 对不确定问题提供可选方案并给出推荐项与权衡。
4. `[MUST]` 对关键假设显式说明。

## 三、任务执行
1. `[MUST]` 先给执行方案再实施；若用户明确要求“直接执行”，可直接落地并回报验证结果。
2. `[MUST]` 小步执行，每步完成后验证结果。
3. `[MUST]` 遇到阻塞时切换策略并继续，不得静默停止。
4. `[SHOULD]` 任务量较大时分阶段推进，并说明每阶段目标。
5. `[MUST]` 审查代码时先给审查方案，再执行审查并输出发现。

## 四、文件与目录操作
1. `[MUST]` 仅修改任务范围内文件。
2. `[MUST]` 禁止破坏性操作（如 `git reset --hard`、`git checkout --`），除非用户明确授权。
3. `[MUST]` 不得回滚或覆盖与当前任务无关的用户改动。
4. `[SHOULD]` 批量同步前先执行差异检查或 dry-run。
5. `[MUST]` 文档与规则链接优先使用相对路径，避免硬编码本地绝对路径（用户明确要求除外）。

## 五、代码生成与质量
1. `[MUST]` 禁止出现中文乱码或不可读编码文本。
2. `[MUST]` 禁止硬编码密钥、Token、密码和私有凭据。
3. `[MUST]` 变更后执行最小可行验证（测试、静态检查或命令验证）。
4. `[SHOULD]` 遵循最小改动原则，避免与任务无关的重构。
5. `[SHOULD]` 对复杂逻辑补充必要注释，注释重点解释“为什么”。

## 六、技能调用

### 总则
- `[MUST]` 接到任何编码任务时，先扫描技能分类表；匹配则必须读取对应 `SKILL.md` 并遵守其指令。
- `[MUST]` 标记为”强制”的技能在对应阶段或领域必须触发。
- `[MUST]` 领域强制技能：只要涉及该领域的开发，就必须调用对应技能进行约束。

### 技能分类与触发时机
> **技能按功能领域组织，标记强制级别和触发条件。**

#### 元编排与调度（复杂项目或需要全流程指导时）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **Meta Dispatcher & Task Orchestrator** | 接收 PRD 或复杂需求时 | 强制（复杂项目） |
| **universal-dev-team** | 初学者或需要全流程指导时 | 按需 |

#### 需求分析（任务开始时）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **brainstorming** | 创造性工作之前 | 强制（创造性任务） |
| **request-analyzer** | 对话开始时 | 强制（所有任务） |

#### 架构设计（新项目或重大迭代时）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **Tech Stack Selector & Architect** | 技术栈选择或架构设计时 | 强制（新项目/重大迭代） |
| **api-design-principles** | 设计或评审 API 时 | 强制（API 设计） |

#### 发现与搜索（查找资源或技能时）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **GitHub Search & Discovery** | 查找开源库或参考案例时 | 按需 |
| **find-skills** | 用户询问"如何做某事"时 | 按需 |

#### 前端开发（涉及前端必须触发）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **frontend-code-review** | 审查前端文件时 | 强制（前端审查） |
| **frontend-design** | 构建 Web 组件、美化 UI 时 | 强制（前端 UI） |
| **ui-ux-pro-max** | 规划/设计/优化 UI/UX 时 | 强制（UI/UX） |
| **vercel-react-best-practices** | 编写/审查 React/Next.js 代码时 | 强制（React） |
| **web-design-guidelines** | 审查 UI 或检查无障碍时 | 强制（无障碍审查） |

#### 后端开发（涉及后端必须触发）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **Backend Database Expert** | 数据库设计或 SQL 优化时 | 强制（数据库） |
| **Backend Node.js Expert** | Node.js 后端开发时 | 强制（Node.js） |
| **Backend Python Expert** | Python 后端开发时 | 强制（Python） |
| **mcp-builder** | 构建 MCP Server 时 | 强制（MCP） |

#### 全栈开发（涉及全栈必须触发）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **fullstack-developer** | 涉及前后端+数据库的全栈开发时 | 强制（全栈） |

#### 测试与自动化（涉及测试必须触发）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **browser-use** | 需要 Web 测试或数据提取时 | 强制（Web 交互） |
| **playwright** | 需要浏览器自动化时 | 强制（浏览器自动化） |
| **test-driven-development** | 实现新功能或修复 Bug 之前 | 强制（所有实现） |
| **webapp-testing** | 测试本地 Web 应用时 | 强制（Web 测试） |

#### 代码审查（完成后必须触发）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **requesting-code-review** | 完成任务或代码合并前 | 强制（所有任务） |

#### 部署与 CI/CD（涉及部署必须触发）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **cloudflare-deploy** | 部署到 Cloudflare 时 | 强制（Cloudflare） |
| **docker-ci-workflow** | 设置 Docker CI/CD 时 | 强制（Docker CI/CD） |

#### 安全（涉及安全必须触发）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **Security Specialist** | 设计认证系统或安全审查时 | 强制（安全审查） |

#### 办公文档（处理文档时）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **docx** | 处理 Word 文档时 | 按需 |
| **pdf** | 处理 PDF 文档时 | 按需 |
| **xlsx** | 处理 Excel 表格时 | 按需 |

#### 文档查询（查询文档时）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **openai-docs** | 咨询 OpenAI 产品或 API 时 | 强制（OpenAI 查询） |

#### 技能管理（管理技能时）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **codex-skills-sync** | 同步技能库时 | 按需 |

#### 多智能体编排（复杂编排时）

| 技能 | 触发条件 | 强制级别 |
|------|----------|----------|
| **trae-deepagent** | 复杂多智能体编排时 | 强制（复杂编排） |
## 七、特定领域 / 工作流

### Skill 技能编写
- `[MUST]` `SKILL.md` Frontmatter 中 `description` 使用中文，确保中文语境触发准确。
- `[MUST]` 从外部仓库迁移技能时，在技能根目录创建 `.upstream.yaml` 记录来源：
  ```yaml
  upstream_url: <原始 GitHub 仓库的具体目录或文件 URL>
  last_synced: <YYYY-MM-DD>
  notes: "记录本地化修改，如翻译或改写 description"
  ```
- `[SHOULD]` `SKILL.md` 正文及脚本/资源可保留英文，不强制全文中文化。

### Python
- `[MUST]` 遵循 `CONVENTIONS.md` 的 Python 工具链和质量要求。
- `[SHOULD]` 新增依赖时固定版本并保证可复现。

### TypeScript
- `[MUST]` 遵循 `CONVENTIONS.md` 的 TypeScript/Node 规范（含 `strict`）。
- `[SHOULD]` 优先使用 ESM 与一致化 lint/format 配置。

### Shell
- `[MUST]` Windows 使用 PowerShell，Linux/macOS 使用 Bash 并启用严格错误处理。
- `[SHOULD]` 跨平台自动化优先用 Python 降低平台差异。

## 八、安全红线
1. `[MUST]` 不在仓库提交任何密钥、Token、密码、私有配置。
2. `[MUST]` 不输出可导致越权的执行指令或绕过权限方案。
3. `[MUST]` 不在未授权情况下执行破坏性数据操作。
4. `[SHOULD]` 高风险操作前做二次确认或 dry-run。

## 九、踩过的坑（持续维护）
- 当前无已登记条目。
- `[MUST]` 出现重复问题后，补充“问题-原因-规避策略”记录。

## 十、临时规则
- 当前无临时规则。
- 新增临时规则时，必须写明：规则内容、生效日期、失效条件。
