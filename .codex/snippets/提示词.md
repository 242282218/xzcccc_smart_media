# 提示词集合

$env:ANTHROPIC_BASE_URL = "https://dawclaudecode.com"
$env:ANTHROPIC_AUTH_TOKEN = "sk-XB5cSriePPJYJIeLG1BxCCGLan6RWvzNQ0A7cHE5TkkCTiJm"


**[强制]** 必须先扫描技能列表，有匹配技能必须触发。  
**[强制]** 必须根据任务类型调用对应的专业智能体，不得由主 Agent 直接处理所有任务。

# 在目标项目根目录执行
if (Test-Path .codex) { Rename-Item .codex ".codex.bak_$(Get-Date -Format yyyyMMddHHmmss)" }
New-Item -ItemType Junction -Path .codex -Target "$env:USERPROFILE\.codex"


为trae注册项目skills，C:\Users\24228\Desktop\qsm\.codex\skills，不调用skill-creator

request-analyzer分析：

使用技能 trae-deepagent 执行agent并发执行任务。

目标：
重点优化视觉一致性：
- 检查颜色体系、字体字号、间距系统、按钮风格、卡片/表单/导航一致性
- 输出不一致点列表
- 设计一套 design token（颜色/字体/间距）
- 生成可复用全局样式文件

硬约束：
1) 必须先输出 01-orchestration-prompts.md（可直接复制运行）
2) 再输出方案、任务卡、依赖与验收文件
3) 若仓库无前端源码，必须明确不可审计原因，但仍输出 token 与全局样式
4) 所有输出 UTF-8，禁止乱码和 TBD/TODO
5) 每次写入新 run 目录，不覆盖历史


---

## 一、代码审查类

### 1.1 全面代码审查
```
全面审查代码，先设计方案，再执行，每个阶段用不同 skills 和智能体，分前端和后端和 api 接口部分，忽略安全问题，我是本地使用

冗余文件：全面审查代码，有哪些冗余文件可以删除，先设计方案，再执行，每个阶段用不同 skills 和智能体，分前端和后端和 api 接口部分，忽略安全问题，我是本地使用


性能方向：全面审查代码，有哪些性能方向上的优化，先设计方案，再执行，每个阶段用不同 skills 和智能体，分前端和后端和 api 接口部分，忽略安全问题，我是本地使用



api接口废弃：全面审查代码，有哪些api接口废弃未使用，先设计方案，再执行，每个阶段用不同 skills 和智能体，分前端和后端和 api 接口部分，忽略安全问题，我是本地使用

```

### 1.2 分模块代码审查
```
分前端和后端和 api 接口部分审查代码，必须调用不同的智能体，每个智能体用对应的 skill，先设计方案，再执行
```

### 1.3 代码审查与修复验证
```
验证问题是否已经修复
```

---

## 二、代码优化类

### 2.1 分模块代码优化
```
分前端和后端和 api 接口部分优化项目，必须调用不同的智能体，每个智能体用对应的 skill，并发运行，而且要保证功能正确不出错
```
整条规则链路（入口文件 -> 执行框架 -> 强制规则 -> 编码约定）
---

## 三、需求分析与设计类

### 3.1 需求理解与确认
```
先理解我的需求，提出你的理解和建议，确认细节
```

### 3.2 提示词优化
```
优化一下我的提示词，不做任何事情，以下是提示词
```

### 3.3 技能设计
```
那你帮我设计一套的 skills，我以后也要用，保存在 C:\Users\24228\Desktop\qsm\.skills-engine\skills，
请先询问我细节，以及说明你的限制和流程后再创建 skills
```

---

## 四、调试与错误排查类

### 4.1 项目调试分析
```
全面分析项目，进行 debug，定位错误原因，给出修复建议
功能：
报错信息：
```

### 4.2 Debug 审查与修复
```
进行 debug 审查，并给出修复建议。
出现问题的地方：
报错信息：
```

### 4.3 网盘状态验证错误示例
```
进行 debug 审查，并给出修复建议。
出现问题的地方：网盘状态验证
报错信息：1.没进度，2.报错 Validation Error
```

---

## 五、部署与 CI/CD 类

### 5.1 GitHub Actions Docker 部署测试
```
帮我在 github-action 测试 docker 部署，调用 docker-ci-workflow，测试完给出拉取链接
```

### 5.2 多标签镜像部署
```
用不同的镜像标签部署
```

---

## 六、快速参考表

| 分类 | 用途 | 典型场景 | 关键词 |
|------|------|----------|--------|
| **代码审查** | 审查代码质量、性能、规范 | 全面审查、分模块审查、修复验证 | `审查 `、` 智能体 `、`skill` |
| **代码优化** | 优化代码结构、性能、并发 | 分模块优化、并发处理 | `优化 `、` 并发 `、` 功能正确` |
| **需求分析** | 理解需求、设计方案、提示词优化 | 需求确认、提示词优化、技能设计 | `理解 `、` 建议 `、` 确认 `、` 设计` |
| **调试排查** | 定位错误、分析原因、修复建议 | 项目 debug、错误定位、修复验证 | `debug`、` 错误原因 `、` 修复 `、` 报错` |
| **部署 CI/CD** | Docker 部署、镜像管理、GitHub Actions | Docker 测试、多标签部署 | `github-action`、`docker`、` 标签` |



**最后更新**: 2026-02-28  
**文件位置**: `C:\Users\24228\Desktop\qsm\.skills-engine\snippets\提示词.md`



参考玻璃拟态 (Glassmorphism)、网格背景效果、噪点纹理整体美化项目

参考滚动触发、悬停微交互动画、按钮交互、交互图标动画、文字动画、3D 透视效果、响应式布局优化交互设计