分析我需求，不明确的提问:
1. [强调]用前端skills和agent
2. 



你是资深前端架构师。

目标：
统一整个项目所有页面的视觉风格、布局结构、组件规范和交互逻辑，
提升一致性、可维护性和可扩展性。

要求：
1. 先分析当前项目结构
2. 输出问题清单（不一致点）
3. 设计统一规范
4. 再进行重构
不要直接改代码，必须先给设计方案。



重点优化视觉一致性：

检查：
- 颜色体系是否统一
- 字体、字号是否统一
- 间距系统（padding/margin）是否统一
- 按钮风格是否统一
- 卡片、表单、导航栏是否统一

输出：
1. 不一致点列表
2. 设计一套 design token 方案（颜色/字体/间距）
3. 统一成可复用的全局样式文件



重点优化布局一致性：

检查：
- 页面容器宽度是否统一
- Header/Footer 是否复用
- 栅格系统是否混乱
- 页面是否存在重复布局代码

输出：
1. 布局结构问题分析
2. 统一 Layout 组件设计
3. 重构为统一布局架构




统一交互逻辑规范：

检查：
- loading 状态是否统一
- 错误提示是否统一
- 表单验证方式是否统一
- 异步请求处理方式是否统一

输出：
统一交互规范
并重构为统一的请求/状态管理方式





你是资深前端架构师，擅长大型项目架构治理与一致性重构。

目标：
统一整个项目所有页面的视觉风格、布局结构、组件规范和交互逻辑，
提升一致性、可维护性、可扩展性和架构清晰度。

————————————
【强制执行流程】

1. 先分析当前项目结构
2. 输出完整问题清单（列出所有不一致点）
3. 设计统一规范方案
4. 确认方案后再进行重构

禁止：
- 不得直接修改代码
- 不得跳过问题分析
- 不得先改代码再解释
- 必须先给完整设计方案

————————————
【一、视觉一致性优化】

需要检查：
- 颜色体系是否统一
- 字体与字号是否统一
- 间距系统（padding/margin）是否统一
- 按钮风格是否统一
- 卡片 / 表单 / 导航栏是否统一
- 阴影 / 圆角 / 边框风格是否统一

输出内容：

1）不一致点列表（逐条列出问题）

2）设计统一 Design Token 方案：
必须包含：
- 主色 / 辅助色 / 状态色 / 中性色
- 字号层级体系
- 行高与字重规范
- 间距递进规则（建议 4px 或 8px 体系）
- 圆角规范
- 阴影规范

示例结构：

export const tokens = {
  colors: {},
  spacing: {},
  fontSize: {},
  lineHeight: {},
  radius: {},
  shadow: {}
}

3）生成统一全局样式方案：
- 全局样式文件结构
- 变量替换方案
- 防止样式失控的治理策略

————————————
【二、布局一致性优化】

需要检查：
- 页面最大宽度是否统一
- 是否存在多个 Layout 实现
- Header / Footer 是否复用
- 栅格系统是否混乱
- 是否存在重复布局代码
- 是否存在硬编码宽度

输出内容：

1）布局结构问题分析（列出重复、不规范、隐式依赖）

2）统一 Layout 架构设计：
- 顶级 AppLayout 结构
- 子布局划分规则
- 容器最大宽度规范
- 栅格系统规范
- Header / Sidebar / Footer 组件规范

示例目录结构：

/layouts
  ├── AppLayout.tsx
  ├── DashboardLayout.tsx
  └── AuthLayout.tsx

3）重构策略：
- 分阶段迁移方案
- 避免一次性重构
- 保持功能稳定

————————————
【三、交互逻辑统一规范】

需要检查：
- loading 状态是否统一
- error 提示是否统一
- toast 是否统一
- 表单验证方式是否统一
- 异步请求处理方式是否统一
- 状态管理是否混乱

输出内容：

1）统一交互规范：
- 全局 Loading 机制
- 请求拦截规范
- 错误处理规范
- 表单校验统一方案
- Toast / Modal 统一封装规范

2）统一请求与状态管理方案：
- 请求层封装结构
- API 分层结构
- 状态管理方案建议
- 推荐目录结构

示例：

/services
  ├── http.ts
  ├── user.service.ts
  └── order.service.ts

————————————
【最终必须输出】

1. 项目结构分析
2. 不一致点完整清单
3. 统一设计规范方案
4. 统一 Layout 架构设计
5. 统一交互规范
6. 分阶段重构实施计划

————————————
【架构强约束】

- 所有样式必须 token 化
- 禁止页面级随意样式定义
- 组件必须可复用、可扩展
- 状态必须可预测
- 结构必须长期可维护





你是资深前端设计工程师。请为我的站点设计一个与 https://skillsmp.com/zh/timeline 同款气质的页面风格（terminal/CLI 叙事式 UI），并输出可运行的实现。

目标风格（必须匹配）：
- 整体像在终端里操作：大量使用 `$ 命令` + “输出结果”的排版；标题用 `# >`、提示用 `[INFO]`、过程用 `loading...` 这类终端语法来表达信息层级。
- 导航与状态展示像 CLI：`$pwd:` 显示路径面包屑；筛选控件表现为 flags：`--granularity daily|weekly|monthly`、`--chart area|bar`、`--cumulative on|off`。
- 视觉：深色底（接近终端背景），高对比文字；关键数值更醒目；细分隔线像终端分割符；整体极简但信息密度高。
- 交互：切换 flags 立即刷新“输出区”（统计与图表）；支持键盘操作（Tab 聚焦、Enter 切换、/ 打开命令面板可选）；提供 Copy 按钮复制当前命令（如 `$ plot timeline.data`）。

实现要求：
- 技术栈：Next.js + Tailwind（可用 shadcn/ui 组件），图表用 Recharts（area/bar），不引入复杂状态库。
- 页面结构固定为三段：
  1) 顶部 Terminal Topbar：左侧显示 “ready ~/ <project>”，右侧放 4 个“命令按钮”（如 $ cd /categories、$ watch stats、$ man docs、$ api --docs），以及语言切换（ZH/EN）。
  2) 中部命令区：显示 `$pwd:` + 面包屑；再显示主命令（如 `$ git log --oneline --graph`）与页面标题 `# > 技能时间线`，并附带一句“像注释”的说明。
  3) 下部输出区：先渲染 flags 控件行；再渲染统计（总数/平均/峰值/峰值日期）；再渲染 `$ plot ...` + `loading...` 行与图表；最后渲染 `$ cat README.md` 的文本块与页脚链接。
- 视觉细节：
  - 全站 monospace（如 ui-monospace / SF Mono 风格），行高略大，留出“终端呼吸感”。
  - 交互态：hover/focus 有轻微高亮/描边；卡片像终端面板（低层阴影或细边框即可）。
  - 文本块（README/输出）要保留 code-block 风格：等宽、可滚动、可复制。
- 数据与状态（可用 mock）：
  - granularity 默认 weekly；chart 默认 area；cumulative 默认 on。
  - 统计展示：总技能数、平均每周期、峰值时期、峰值日期（可用示例值）。
  - 图表数据：按时间序列生成 mock（至少 30 个点），切换 granularity 会改变点数与聚合逻辑（简单聚合即可）。
  - cumulative on 时显示累计曲线；off 显示当期值。

输出内容（必须给全）：
1) 设计说明（简短，列出布局与交互规则）
2) 完整代码（可直接运行）：页面组件、mock 数据、状态逻辑、样式
3) 验收清单：逐条对应“终端叙事、flags 控件、统计输出、plot 输出、键盘可用性、响应式”
注意：不要做传统营销网站风格；不要大图插画；不要花哨渐变。核心是“像在终端里看数据与操作”的克制感与高信息密度。