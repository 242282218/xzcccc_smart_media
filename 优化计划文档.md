# 项目优化计划（Optimization Plan）

## 1. 计划目标
- 在不破坏现有核心功能的前提下，优先修复安全与稳定性高风险问题。
- 将测试状态从当前“14 failed / 108 passed / 10 skipped”推进到“主干全绿（0 fail）”。
- 建立可持续的质量防线：鉴权一致性、配置安全、路径策略一致性、回归测试门禁。

## 2. 当前基线（2026-02-07）
- 代码审查结论：存在认证覆盖不一致、敏感信息暴露、默认弱口令等问题。
- 测试现状：`pytest -q` 结果为 `14 failed, 108 passed, 10 skipped`。
- 主要失败簇：
  1. 异步测试环境配置缺失（pytest-asyncio）
  2. 路径校验策略升级后，测试/接口预期未同步
  3. 个别接口签名变更与测试调用不一致
  4. 智能重命名相关行为与断言不一致

## 3. 优化范围
- 后端：`quark_strm/app/api`、`quark_strm/app/core`、`quark_strm/app/services`
- 前端：`quark_strm/web/src`
- 测试：`quark_strm/tests`
- 文档：`docs` 与项目根目录质量文档

## 4. 分阶段实施计划

### Phase 0：止血与门禁（1-2 天）
**目标**：先把高风险问题“关闸”，避免继续扩散。

1. 鉴权一致性收敛
- 对敏感路由统一应用 `Depends(require_api_key)`，优先覆盖：
  - `/api/proxy/*`（含 stream/redirect/transcoding）
  - `/api/notification` 的读取接口（channels/rules/logs）
  - `/config` 及系统配置读取接口
- 引入“路由级默认鉴权 + 少量白名单豁免”模式，避免遗漏。

2. 移除弱口令默认值
- 禁止 WebDAV 运行时 `admin/password` 回退；未配置即拒绝启动该能力并给出明确错误。
- 前端 WebDAV 默认表单去除弱口令，使用空值并要求显式填写。

3. CORS 和敏感数据输出收口
- 删除流媒体代理中硬编码 `Access-Control-Allow-Origin: *`。
- 通知配置返回结构进行脱敏（token/secret/password 仅返回 masked）。

**验收标准**
- 未授权请求访问上述敏感接口返回 `401/403`。
- 代码中不再出现 `admin/password` 回退逻辑。
- 通知接口不返回明文敏感配置。

---

### Phase 1：测试恢复与策略对齐（2-3 天）
**目标**：将失败用例从 14 降至 0，恢复主干可回归状态。

1. 异步测试环境修复
- 安装并固定 `pytest-asyncio`。
- 增补/校正 `pytest.ini`：`asyncio_mode=auto`（或按项目约定）。

2. 路径策略一致化
- 明确规范：接口层是只接受相对路径，还是允许绝对路径但限制在 allowed dirs。
- 按规范统一修改：
  - 校验器（`validators.py`）
  - API 参数说明与错误码
  - 相关测试输入和预期（422/400/200）

3. 接口签名与测试同步
- 修复 `clear_failed_records` 等参数不匹配问题。
- 逐条清理失败用例断言，避免“实现更新，测试未跟进”。

4. 智能重命名行为对齐
- 对 `test_ai_parse_robustness` 与 API flow 用例做“期望-实现差异表”，二选一：
  - 调整业务逻辑回归预期
  - 更新断言适配当前合理行为

**验收标准**
- `pytest -q` 全量通过（允许有明确标注的 skip）。
- 新增至少 1 组鉴权负例测试（未授权必须失败）。

---

### Phase 2：架构与可维护性优化（3-5 天）
**目标**：减少后续返工，提升可维护性与可观测性。

1. 异常处理分层
- 缩减 `except Exception` 大捕获范围，按业务错误类型返回稳定错误码。
- 统一错误响应结构（错误码、message、request_id、上下文）。

2. 安全配置治理
- 建立敏感配置读取/输出统一网关（强制脱敏、最小暴露）。
- 前端认证存储从 `localStorage` 逐步迁移到更安全方案（HttpOnly Cookie 或短期内存令牌）。

3. 路由与权限映射清单化
- 生成“路由-权限矩阵”文档，纳入 CI 校验（新增路由必须声明权限级别）。

4. 监控与审计
- 增加关键安全事件日志：未授权访问、配置变更、鉴权失败频率。
- 增加 smoke 脚本：启动后自动探测核心接口健康与权限边界。

**验收标准**
- 关键模块鉴权覆盖率达到 100%。
- 新增路由若未声明权限，CI 失败。
- 错误码与日志结构在 API 文档中可追踪。

---

## 5. 任务拆解（WBS）
1. 安全修复
- [ ] 统一路由鉴权依赖
- [ ] 通知读取接口脱敏
- [ ] 移除 WebDAV 默认弱口令
- [ ] 取消代理端点通配 CORS

2. 测试修复
- [ ] 配置 pytest-asyncio
- [ ] 修复路径相关失败用例
- [ ] 修复签名不匹配用例
- [ ] 修复智能重命名断言

3. 质量门禁
- [ ] 增加鉴权负例测试
- [ ] 增加路由权限矩阵检查
- [ ] 增加最小 smoke 测试

4. 文档更新
- [ ] 更新 API 权限说明
- [ ] 更新路径输入规范
- [ ] 更新测试执行与故障排查文档

## 6. 里程碑与交付物
- M1（D+2）：安全止血完成
  - 交付物：鉴权补丁、脱敏补丁、弱口令移除补丁
- M2（D+5）：测试恢复完成
  - 交付物：全量测试通过报告、失败归零记录
- M3（D+10）：质量防线落地
  - 交付物：权限矩阵、CI 门禁规则、监控告警补充

## 7. 风险与应对
1. 风险：路径策略调整影响线上兼容性
- 应对：先加兼容层和告警，再分阶段收紧；提供迁移说明。

2. 风险：鉴权补齐后影响现有调用方
- 应对：提供临时兼容窗口与变更公告；灰度开启。

3. 风险：测试改动过大导致“假绿”
- 应对：保留负例、回归、集成三类用例，不以删测换通过。

## 8. 成功判定（DoD）
- 安全：高危问题全部关闭，无明文敏感信息泄露路径。
- 质量：主干 `pytest -q` 0 fail。
- 规范：新增接口必须满足“权限声明 + 测试覆盖 + 文档更新”。
- 运维：关键行为有日志可追踪，异常可快速定位。

## 9. 推荐执行顺序（简版）
1. 先改鉴权和默认口令（安全止血）
2. 再修测试环境和失败用例（恢复回归）
3. 最后补 CI 门禁和文档（防止复发）
