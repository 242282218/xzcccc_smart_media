# 配置文件目录

本目录包含项目的各种配置文件。

## 文件说明

- `default.yaml` - 默认配置文件
- `.env.example` - 环境变量配置示例
- `telegram_channels.json` - Telegram频道配置

## 使用说明

1. 复制 `.env.example` 为 `.env` 并填写实际配置
2. 根据需要修改 `default.yaml` 中的配置项
3. Telegram相关配置请参考 `telegram_channels.json` 格式