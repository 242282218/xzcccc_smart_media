# 部署清单

生成时间：2026-02-07
项目根目录：`c:\Users\24228\Desktop\smart_media`

## 1. 已完成清理

### 1.1 日志清理
- 已删除根目录 `logs/` 下非关键日志，当前仅保留 `logs/.gitkeep`
- 已删除 `quark_strm/logs/` 下全部 `*.out.log`、测试日志、历史运行日志与报告文件
- 已保留关键错误日志：`quark_strm/logs/**/*.err.log`
- 未删除项（文件占用中）：`quark_strm/logs/quark_strm.log`

### 1.2 缓存清理
- 已删除：`.pytest_cache/`
- 已删除：`node_modules/`
- 已删除：`quark_strm/.pytest_cache/`
- 已删除：`quark_strm/web/node_modules/`
- 已删除：`quark_strm/web/dist/`
- 已删除：`quark_strm/__pycache__/` 及其子目录缓存
- 已删除：`.tmp_rules_install/`、`.tmp_rules_repos/`

### 1.3 文件清理
- 已删除测试目录：`quark_strm/tests/`、`quark_strm/strm_test/`
- 已删除测试脚本：`scripts/auto_test.py`、`scripts/rename_cloud_test.py`、`quark_strm/test_*.py`、`quark_strm/verify_*.py`
- 已删除验证脚本目录：`quark_strm/scripts/verification/`
- 已删除临时目录：`quark_strm/tmp/`
- 已删除测试编排文件：`docker-compose.test.yml`
- 已删除备份/临时配置：`quark_strm/config.yaml.bak`、`quark_strm/config.yaml.localtmp`
- 已删除调试资源：`quark_strm/web/public/debug-api.html`

## 2. 项目结构整理结果

当前保留的核心结构：
- 后端代码：`quark_strm/app/`
- 前端代码：`quark_strm/web/`
- 脚本：`quark_strm/scripts/`
- 文档：`quark_strm/docs/`
- 数据与媒体：`quark_strm/data/`、`quark_strm/strm/`
- 运行目录：`logs/`、`output/`、`quark_strm/logs/`、`quark_strm/output/`（已补 `.gitkeep`）

## 3. 部署必需文件核验

以下关键文件已确认存在：
- `quark_strm/app/main.py`
- `quark_strm/requirements.txt`
- `quark_strm/Dockerfile`
- `quark_strm/docker-compose.yml`
- `quark_strm/config.example.yaml`
- `quark_strm/web/package.json`
- `quark_strm/web/src/main.ts`

## 4. 清理规模

按清理前扫描的目录体积估算，已释放至少约 `405700977` 字节（约 `386.9 MiB`），不含部分日志与零散文件差异。

## 5. 最终部署前检查项

- 处理占用文件：`quark_strm/logs/quark_strm.log`（停止占用进程后删除）
- 重新安装依赖：
  - 根目录：`npm install`
  - 前端目录：`cd quark_strm/web && npm install`
  - Python 依赖：`pip install -r quark_strm/requirements.txt`
- 执行部署前最终自检（服务启动、关键接口、前端构建）

